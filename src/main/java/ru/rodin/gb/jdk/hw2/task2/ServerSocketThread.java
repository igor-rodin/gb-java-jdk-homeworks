package ru.rodin.gb.jdk.hw2.task2;

public class ServerSocketThread {

    private ServerSocketThreadListener serverSocketThreadListener;

    public ServerSocketThread(ServerSocketThreadListener serverSocketThreadListener) {
        this.serverSocketThreadListener = serverSocketThreadListener;
    }
}
